
function booWho(bool) {
  // What is the new fad diet for ghost developers? The Boolean.
  
  /* Code Skeleton
  
  1. Return bool and see what are the cases that need work
  
  */
  
  if (bool === true || bool === false) {
    return true;
  } else {
    return false;
  }
}

booWho(null);
